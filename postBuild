# Download data for gradients.ipynb notebook 
wget -O results.zip https://osf.io/kb2ga/download/
unzip -j results.zip -d $HOME/03/

# build jupyter-lab
jupyter lab build --dev-build=False --minimize=False
